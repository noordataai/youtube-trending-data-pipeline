# Runtime dependencies for Lambda functions and local scripts.
# Note: AWS Glue jobs run in the Glue runtime and already have boto3,
# awswrangler, and PySpark available — this file is for Lambda packaging
# and for running scripts/ locally.

boto3>=1.34
awswrangler>=3.7
pandas>=2.1

# Dev / lint tooling (used by CI, see .github/workflows/ci.yml)
flake8>=7.0
black>=24.0
